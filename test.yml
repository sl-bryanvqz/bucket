# Load dataframe into target Delta table
def load_to_target(source_df_cleaned, target_table_path, target_load_strategy):

    # Create target path if this is the first load
    if not DeltaTable.isDeltaTable(spark, target_table_path):
        rows_inserted = source_df_cleaned.count()
        source_df_cleaned.write.format("delta").mode("overwrite").save(target_table_path)

        return {
            "rows_inserted": rows_inserted,
            "rows_updated": 0,
            "rows_affected": rows_inserted,
            "merge_skipped": False
        }

    # Get existing target table
    delta_table = DeltaTable.forPath(spark, target_table_path)

    # Merge SCD1: insert new rows and update only changed rows
    if target_load_strategy == "merge-scd1":
        target_df = delta_table.toDF().select("_hashed_pk", "_hashed_row")
        source_df = source_df_cleaned.select("_hashed_pk", "_hashed_row")

        rows_to_insert = source_df.join(target_df, "_hashed_pk", "left_anti").count()

        rows_to_update = (
            source_df.alias("s")
            .join(target_df.alias("t"), "_hashed_pk", "inner")
            .filter("NOT (t._hashed_row <=> s._hashed_row)")
            .count()
        )

        if rows_to_insert == 0 and rows_to_update == 0:
            return {
                "rows_inserted": 0,
                "rows_updated": 0,
                "rows_affected": 0,
                "merge_skipped": True
            }

        (
            delta_table.alias("t")
            .merge(source_df_cleaned.alias("s"), "t._hashed_pk = s._hashed_pk")
            .whenMatchedUpdateAll(condition="NOT (t._hashed_row <=> s._hashed_row)")
            .whenNotMatchedInsertAll()
            .execute()
        )

        return {
            "rows_inserted": rows_to_insert,
            "rows_updated": rows_to_update,
            "rows_affected": rows_to_insert + rows_to_update,
            "merge_skipped": False
        }

    # Overwrite target table
    if target_load_strategy == "overwrite":
        rows_inserted = source_df_cleaned.count()
        source_df_cleaned.write.format("delta").mode("overwrite").option("overwriteSchema", "true").save(target_table_path)

        return {
            "rows_inserted": rows_inserted,
            "rows_updated": 0,
            "rows_affected": rows_inserted,
            "merge_skipped": False
        }

    # Append to target table
    if target_load_strategy == "append":
        rows_inserted = source_df_cleaned.count()
        source_df_cleaned.write.format("delta").mode("append").option("mergeSchema", "true").save(target_table_path)

        return {
            "rows_inserted": rows_inserted,
            "rows_updated": 0,
            "rows_affected": rows_inserted,
            "merge_skipped": False
        }

    # Stop if strategy is not supported
    raise ValueError(f"Unsupported target_load_strategy: {target_load_strategy}")
